---
title: Социальная Система - Семьи, Демография, Классы
description: Описание социальной системы симуляции, включающей структуру семей, демографическую динамику, образование классов, классовое сознание, наследование и социальное воспроизводство
keywords:
  - общество
  - семья
  - родство
  - демография
  - классовая система
  - классовое сознание
  - наследование
  - социальное воспроизводство
  - класс
  - марксизм
  - родственные связи
lang: ru
---

# Социальная Система - Семьи, Демография, Классы

## Обзор

Социальная система (модуль `society`) — это **структурная надстройка**, которая возникает на основе экономических отношений (базиса). По Марксу, социальная структура общества определяется способом производства и собственностью на средства производства.

**Ключевые компоненты:**
- **FamilySystem** — система семей и родственных связей
- **DemographySystem** — управление рождаемостью, смертностью, старением
- **ClassSystem** — система классов, формирующаяся из экономических отношений
- **Genetics** — наследование черт от родителей (интегрирована с NPC системой)
- **Inheritance** — передача собственности по наследству (интегрирована с property системой)

**Философский контекст:**

Социальная система воплощает принцип исторического материализма: классовые отношения не являются естественной константой, а **возникают историчес из материальных условий**. На этапе примитивной коммуны нет классов. По мере развития частной собственности (на землю, скот, орудия) появляются классы: эксплуататоры (владельцы) и эксплуатируемые (не владеющие).

---

## 1. Система Семей и Родства

### 1.1. Философская основа

Семья — это не просто биологическое понятие, но социальный институт, форма которого определяется экономическими условиями. Как писал Энгельс в "Происхождении семьи, частной собственности и государства":

> "Определяющим фактором в истории является, в конечном счёте, производство и воспроизводство непосредственной жизни. Но это, в свою очередь, имеет различный характер. С одной стороны, естественное воспроизводство — продолжение рода человеческого, с другой стороны, производство средств к жизни, труда, пищи, одежды, жилища и необходимых для этого орудий." — Ф. Энгельс

**В симуляции семья:**
1. **Определяет наследование** — передача собственности и статуса
2. **Влияет на социальные связи** — семьи образуют кланы и династии
3. **Формирует идентичность** — принадлежность к роду определяет судьбу
4. **Воспроизводит классовые отношения** — дети наследуют класс родителей

### 1.2. Структура семейной системы

#### **Типы Родства (KinshipType)**

Система отслеживает следующие типы родственных связей:

```python
class KinshipType(Enum):
    """Типы родства"""
    PARENT = "родитель"
    CHILD = "ребёнок"
    SPOUSE = "супруг"              # Муж или жена
    SIBLING = "брат/сестра"
    GRANDPARENT = "дед/бабка"
    GRANDCHILD = "внук"
    UNCLE_AUNT = "дядя/тётя"
    NEPHEW_NIECE = "племянник"
    COUSIN = "двоюродный"
```

**Ключевые особенности:**
- Все связи **двусторонние** — если A родитель B, то B ребёнок A
- Система отслеживает **кровное родство** (is_blood flag)
- Поддерживаются **свойские связи** через брак (не кровные)

#### **Брачные Системы (MarriageType)**

Система поддерживает различные типы браков в зависимости от эпохи:

```python
class MarriageType(Enum):
    """Типы брака"""
    MONOGAMY = "моногамия"
    POLYGYNY = "полигиния"         # Многожёнство (муж + несколько жён)
    POLYANDRY = "полиандрия"       # Многомужество (жена + несколько мужей)
    GROUP = "групповой"            # Групповой брак (община)
```

В ранних обществах допускается групповой брак (вся молодёжь общины), с развитием переходит к полигинии (когда вождь берёт нескольких жён), затем к моногамии с развитием государства.

#### **Структура Семьи (Family)**

```python
@dataclass
class Family:
    """Семья - группа родственников"""
    id: str                         # Уникальный ID семьи
    name: str                       # Фамилия/название рода

    # Члены семьи
    members: Set[str]               # IDs всех членов (NPC)
    head_id: Optional[str]          # Глава семьи (патриарх/матриарх)

    # Родословная
    ancestors: List[str]            # Известные предки (для истории)

    # Статус
    founding_year: int              # Когда была основана семья
    prestige: float                 # Престиж семьи (0-100)
```

### 1.3. Примеры Использования

**Пример 1: Создание семьи**

```python
family_system = FamilySystem()

# Старейшина племени основывает новую семью
family = family_system.create_family(
    name="Род Охотников",           # Название рода
    founder_id="npc_001",           # ID основателя
    year=0                          # Год создания
)

# Добавляем членов семьи
family_system.families[family.id].add_member("npc_002")  # Жена
family_system.families[family.id].add_member("npc_003")  # Сын
family_system.families[family.id].add_member("npc_004")  # Дочь
```

**Пример 2: Установка родственных связей**

```python
# Отец и сын
family_system.add_kinship(
    "npc_001",                      # Отец
    "npc_003",                      # Сын
    KinshipType.CHILD,              # Тип связи
    is_blood=True                   # Кровная связь
)

# Теперь система знает: npc_001 -> CHILD -> npc_003 и npc_003 -> PARENT -> npc_001

# Получение всех детей
children = family_system.get_relatives("npc_001", KinshipType.CHILD)
# Результат: ["npc_003", "npc_004", ...]
```

**Пример 3: Регистрация брака**

```python
marriage = Marriage(
    id="marriage_001",
    spouse1_id="npc_001",           # Муж
    spouse2_id="npc_002",           # Жена
    year_married=10,
    is_active=True,
    children=[]
)

family_system.marriages["marriage_001"] = marriage
family_system.npc_marriage["npc_001"] = "marriage_001"
family_system.npc_marriage["npc_002"] = "marriage_001"

# Рождение ребёнка
marriage.add_child("npc_003")
```

---

## 2. Демографическая Система

### 2.1. Концепция: Демография как Социальный Процесс

Демография в симуляции **не случайна и не биологически предопределена**, а **социально обусловлена**. Рождаемость и смертность зависят от экономических условий, как это происходит в реальности:

- **Голод** → снижение рождаемости и повышение смертности
- **Война и конфликты** → повышение смертности
- **Технологический прогресс** → снижение детской смертности
- **Достаток** → увеличение рождаемости

### 2.2. Этапы Жизни (LifeStage)

```python
class LifeStage(Enum):
    """Этапы жизни"""
    INFANT = ("младенец", 0, 2)
    CHILD = ("ребёнок", 2, 12)
    ADOLESCENT = ("подросток", 12, 16)
    YOUNG_ADULT = ("молодой", 16, 30)
    ADULT = ("взрослый", 30, 50)
    ELDER = ("старик", 50, 100)
```

Каждый этап имеет свои:
- **Роль в производстве** — дети не работают, взрослые работают
- **Репродуктивный статус** — подростки и молодые взрослые могут иметь детей
- **Смертность** — младенцы имеют самую высокую, старики — растущую
- **Социальный статус** — старики часто получают авторитет

### 2.3. Факторы, Влияющие на Демографию

```python
@dataclass
class DemographicFactors:
    """Факторы, влияющие на демографию"""
    food_availability: float = 1.0      # Доступность еды (0-2)
    health_conditions: float = 1.0      # Здоровье населения (0-1)
    safety: float = 1.0                 # Безопасность (0-1)
    technology_level: float = 0.0       # Уровень развития
    social_stability: float = 1.0       # Социальная стабильность (0-1)
```

**Влияние факторов:**

| Фактор | Рождаемость | Смертность | Примечание |
|--------|------------|-----------|-----------|
| Еда <0.5 | ↓↓ | ↑↑ | Голод убивает |
| Еда 1.5+ | ↑ | ↓ | Достаток способствует размножению |
| Война | ↓ | ↑ | Люди не рожают в страхе |
| Технологии | ↑ | ↓ | Медицина снижает смертность |
| Болезнь | ↓ | ↑↑ | Эпидемии кошмарны |

### 2.4. Рождаемость

**Расчёт вероятности зачатия:**

```python
def calculate_fertility(self, age: int, is_female: bool,
                        has_partner: bool,
                        personal_health: float = 1.0) -> float:
    """Вычисляет вероятность зачатия"""
    # Только женщины с партнёром
    if not is_female or not has_partner:
        return 0.0

    # Фертильный возраст 16-45
    if age < 16 or age > 45:
        return 0.0

    # Возрастной фактор (пик в 20-30)
    if age < 20:
        age_factor = 0.5
    elif age < 30:
        age_factor = 1.0
    elif age < 40:
        age_factor = 0.7
    else:
        age_factor = 0.3

    fertility = base_fertility_rate * age_factor

    # Еда влияет критически
    fertility *= food_availability_factor

    # Здоровье
    fertility *= personal_health

    # Стабильность (люди не рожают в войне)
    fertility *= (0.5 + social_stability * 0.5)

    return fertility
```

**Базовые параметры:**
- Базовая рождаемость: 0.15 (15% женщин за год при условиях)
- Пик рождаемости: 20-30 лет
- Максимальный возраст рождения: 45 лет
- Минимальный возраст партнёра: 16 лет

### 2.5. Смертность

Смертность моделируется через **кривую Гомперца** (exponential mortality with age):

```python
def calculate_mortality(self, age: int, life_stage: LifeStage,
                        health: float = 1.0,
                        is_starving: bool = False) -> Tuple[float, DeathCause]:
    """Вычисляет вероятность смерти"""
    # Кривая Гомперца: смертность экспоненциально растёт с возрастом
    base_mortality = 0.001 * math.exp(0.08 * age)

    # Младенческая смертность 20% (базовая)
    if life_stage == LifeStage.INFANT:
        base_mortality = 0.2
        # Технологии снижают младенческую смертность
        base_mortality *= (1 - technology_level * 0.1)

    # Голод добавляет 10% смертности
    if is_starving:
        base_mortality += 0.1

    # Плохое здоровье
    if health < 0.5:
        base_mortality *= (2 - health)

    # Технологии (медицина) снижают смертность
    tech_reduction = min(0.5, technology_level * 0.05)
    base_mortality *= (1 - tech_reduction)

    return min(1.0, base_mortality), cause
```

**Причины смерти (DeathCause):**

```python
class DeathCause(Enum):
    """Причины смерти"""
    OLD_AGE = "старость"
    DISEASE = "болезнь"
    STARVATION = "голод"
    ACCIDENT = "несчастный случай"
    VIOLENCE = "насилие"
    CHILDBIRTH = "роды"               # Смерть матери при родах (~5%)
    INFANT_MORTALITY = "младенческая смертность"
    COLD = "переохлаждение"
    PREDATOR = "хищник"
```

### 2.6. Возрастная Пирамида

Демографическая структура общества визуализируется через возрастную пирамиду:

**Растущее общество (молодое):**
```
      |
    65+|    ■□
      |
    55+|   ■■□□
      |
    45+|  ■■■□□□
      |
    35+| ■■■■□□□□
      |
    25+|■■■■■□□□□□
      |
    15+|■■■■■■□□□□□□
      |
     5+|■■■■■■■□□□□□□
      |
     0+|■■■■■■■■□□□□□□□
      |__________|
        Мужчины  Женщины
```

**Стабильное общество:**
```
     65+|  ■□
       |
     55+|  ■■□□
       |
     45+|  ■■■□□□
       |
     35+|  ■■■□□□
       |
     25+|  ■■■□□□
       |
     15+|  ■■■□□□
       |__________|
         М      Ж
```

**Сокращающееся общество (старое):**
```
     65+|  ■■■□□□
       |
     55+|  ■■□□
       |
     45+|  ■■□□
       |
     35+|  ■□
       |
     25+|  □
       |__________|
```

### 2.7. Ожидаемая Продолжительность Жизни

```
Период              Ожидание  Примечание
────────────────────────────────────────────
Примитивная община    30-35   60% детей не доживают до 15
Ранний класс. строй   35-40   Достаток позволяет выжить
Средние века          40-45   Медицина, плохие условия
Развитое общество     50+     Медицина, пища, гигиена
```

Расчёт ожидаемой продолжительности жизни:

```python
def calculate_life_expectancy(self) -> float:
    """Вычисляет текущую ожидаемую продолжительность жизни"""
    base = 45  # Примитивное общество

    # Еда
    if food_availability < 0.8:
        base *= (0.5 + food_availability * 0.5)

    # Здоровье (эпидемии)
    base *= (0.7 + health_conditions * 0.3)

    # Безопасность
    base *= (0.8 + safety * 0.2)

    # Технологии (+5 лет за уровень технологии)
    base += technology_level * 5

    return base
```

---

## 3. Система Классов и Классовое Сознание

### 3.1. Философская Основа: Классы как Эмерджентное Явление

В марксистской теории классы **не являются вечными** и **не предопределены**. Они возникают исторически из противоречия между производительными силами и производственными отношениями.

**Этапы развития классов:**

```
Примитивная коммуна (0 ТХ)
└─ Нет частной собственности
└─ Нет классов
└─ Общее владение средствами производства

        ↓ (Возникает частная собственность)

Рабовладельческое общество (~1-10 ТХ)
└─ Господа (владельцы рабов)
└─ Рабы (не владеют ничем)
└─ Классовое сознание: низкое (рабы не осознают единство)

        ↓ (Рабство экономически неэффективно)

Феодальное общество (~11-30 ТХ)
└─ Земельная аристократия
└─ Крестьяне (феодально зависимые)
└─ Пролетарии (городские)
└─ Классовое сознание: растёт (волнения)

        ↓ (Буржуазная революция)

Капиталистическое общество (~31+ ТХ)
└─ Буржуазия (капиталисты)
└─ Пролетариат (рабочие)
└─ Классовое сознание: высокое (революция?)
```

**Ключевой принцип:** На примитивной стадии **нет классов**. Классы возникают, когда:
1. Появляется **частная собственность** на средства производства
2. Образуется **избыточный продукт** (прибавочный продукт)
3. Формируется **государство** для защиты собственности

### 3.2. Типы Классов (ClassType)

```python
class ClassType(Enum):
    """Типы классов (возникают по мере развития)"""
    NONE = ("нет класса", 0)                # Примитивная община
    COMMUNAL_MEMBER = ("общинник", 1)       # Член общины
    LANDOWNER = ("землевладелец", 2)        # Владеет землёй (эксплуататор)
    LANDLESS = ("безземельный", 2)          # Не владеет землёй (эксплуатируемый)
    CRAFTSMAN = ("ремесленник", 2)          # Владеет мастерской
    LABORER = ("работник", 2)               # Работает на других
    ELDER = ("старейшина", 1)               # Общинная элита
    CHIEF = ("вождь", 2)                    # Военный лидер

    @property
    def is_exploiter(self) -> bool:
        """Является ли класс эксплуататорским"""
        return self in [ClassType.LANDOWNER, ClassType.CHIEF]

    @property
    def is_exploited(self) -> bool:
        """Является ли класс эксплуатируемым"""
        return self in [ClassType.LANDLESS, ClassType.LABORER]
```

### 3.3. Механизм Образования Классов

Класс NPC определяется его **отношением к собственности**:

```python
def determine_class(npc: NPC, property_system: OwnershipSystem,
                    economy: EconomySystem) -> ClassType:
    """Определяет класс NPC на основе экономических отношений"""

    # На примитивной стадии все — общинники
    if economy.technology_level < 1:
        return ClassType.COMMUNAL_MEMBER

    # Какой собственностью владеет?
    owned_land = property_system.get_property_by_owner(npc.id, PropertyType.LAND)
    owned_tools = property_system.get_property_by_owner(npc.id, PropertyType.TOOL)

    # Кого нанимает?
    employees = economy.get_employees_of(npc.id)

    # Кому работает?
    employer = economy.get_employer_of(npc.id)

    # Логика определения класса
    if len(owned_land) > 0 and len(employees) > 0:
        # Владеет землёй И нанимает работников → землевладелец (эксплуататор)
        return ClassType.LANDOWNER

    elif len(owned_land) > SUBSISTENCE_LAND and employer is None:
        # Владеет достаточно землёй для прожитка, никому не работает
        return ClassType.LANDOWNER

    elif len(owned_tools) > 0 and employer is None:
        # Владеет ремесленными инструментами, независим
        return ClassType.CRAFTSMAN

    elif employer is not None:
        # Работает на кого-то → работник (эксплуатируемый)
        return ClassType.LABORER

    else:
        # Не имеет собственности → безземельный
        return ClassType.LANDLESS
```

### 3.4. Классовое Сознание (Class Consciousness)

Классовое сознание — это **осознание класса своих интересов и единства** с другими членами класса против других классов.

**Фазы Развития Классового Сознания (по Грамши):**

```python
class ConsciousnessPhase(Enum):
    """Фазы развития классового сознания"""
    NONE = ("отсутствует", 0.0, 0.1)
    ECONOMIC = ("экономическое", 0.1, 0.3)
    CORPORATIVE = ("корпоративное", 0.3, 0.5)
    POLITICAL = ("политическое", 0.5, 0.7)
    HEGEMONIC = ("гегемоническое", 0.7, 1.0)
```

**Описание каждой фазы:**

| Фаза | Осознание | Действия | Пример |
|------|-----------|---------|---------|
| **Отсутствует** | Класс не осознаёт себя как класс | Никаких | Рабы думают, это естественный порядок |
| **Экономическое** | Осознание экономических интересов | Требование лучшей оплаты | Забастовки за зарплату |
| **Корпоративное** | Осознание групповых интересов, солидарность | Организация в группы | Ремесленные цеха, гильдии |
| **Политическое** | Осознание классовых интересов против других классов | Политическая борьба | Революционные организации |
| **Гегемоническое** | Борьба за культурное и идеологическое господство | Создание новой идеологии | Создание нового государства |

**Развитие Классового Сознания:**

```
Начальное состояние
    ↓
Люди переживают экономические трудности
    ↓
Начинают обсуждать, замечают общие проблемы (Экономическое)
    ↓
Организуются в группы (Корпоративное)
    ↓
Осознают противостояние другим классам (Политическое)
    ↓
Начинают борьбу за переустройство общества (Гегемоническое)
```

**Факторы, Влияющие на Развитие Классового Сознания:**

```python
factors_promoting_consciousness = {
    "эксплуатация_интенсивность": +0.05,      # Чем хуже условия, тем выше сознание
    "контакт_с_другими_классами": +0.03,     # Конфликты пробуждают сознание
    "наличие_лидеров": +0.10,                 # Органические интеллектуалы ускоряют
    "пропаганда": +0.08,                      # Идеология, верования
    "успешные_восстания": +0.15,              # Успех вдохновляет
    "снижение_экономического_давления": -0.05,  # Сытость снижает активность
}
```

---

## 4. Система Наследования

### 4.1. Наследование как Социальное Явление

Наследование — это **механизм воспроизводства классовых отношений**. Оно переводит классовое положение родителей на детей, стабилизируя классовую структуру.

Как писал Маркс:

> "Наследование показывает, как капиталист не в состав присвоить результаты чужого труда, как только он отдаёт в руки других исторически полученное богатство." — К. Маркс, "Критика политической экономии"

**Эффекты Наследования:**

```
Богатый отец (земля, деньги)
    ↓
Наследует сын
    ↓
Сын уже владеет средствами производства
    ↓
Может нанимать работников
    ↓
Получает прибавочный продукт ещё до работы
    ↓
Становится богаче
    ↓
Его дети наследуют ещё больше
    ↓
Класс стабилизируется
```

### 4.2. Типы Собственности и Наследуемость

Не вся собственность передаётся по наследству:

```python
class PropertyType(Enum):
    """Типы собственности"""
    LAND = PropertyType(
        name="земля",
        category=PropertyCategory.REAL_ESTATE,
        can_transfer=True,
        inheritable=True,                    # ✓ ПЕРЕДАЁТСЯ
        base_value=1000
    )

    TOOL = PropertyType(
        name="инструмент",
        category=PropertyCategory.MEANS_OF_PRODUCTION,
        can_transfer=True,
        inheritable=True,                    # ✓ ПЕРЕДАЁТСЯ
        base_value=50
    )

    RESOURCE = PropertyType(
        name="ресурс",
        category=PropertyCategory.CONSUMPTION,
        can_transfer=False,
        inheritable=False,                   # ✗ НЕ ПЕРЕДАЁТСЯ (съедается)
        base_value=1
    )

    LIVESTOCK = PropertyType(
        name="скот",
        category=PropertyCategory.MEANS_OF_PRODUCTION,
        can_transfer=True,
        inheritable=True,                    # ✓ ПЕРЕДАЁТСЯ
        base_value=100
    )
```

### 4.3. Процесс Наследования

Когда NPC умирает, его собственность передаётся наследникам в порядке приоритета:

```python
def process_inheritance(self, deceased_id: str, heir_id: str, year: int) -> List[str]:
    """Обрабатывает наследование имущества"""
    inherited = []

    # Находим всю собственность умершего
    for prop in self.properties.values():
        if prop.owner_id == deceased_id:
            # Если наследуемо — передаём наследнику
            if prop.inheritable:
                prop.owner_id = heir_id
                prop.acquisition_method = "наследство"
                prop.last_transfer_date = year
                inherited.append(prop.property_id)

    return inherited
```

**Порядок Наследования:**

```
1. Дети (в порядке рождения)
   └─ Первенец получает большую часть
   └─ Младшие дети получают меньше

2. Если нет детей → Супруг(а)

3. Если нет супруга → Родители

4. Если нет родителей → Братья/сёстры

5. Если одинокий → Общество (государство)
   └─ Земля переходит в государственную собственность
   └─ Инструменты распродаются
```

### 4.4. Влияние Наследования на Неравенство

Наследование **экспоненциально увеличивает неравенство** с течением времени:

**Начальное Состояние (год 0):**
```
Богатство распределено примерно равномерно
Коэффициент Джини = 0.3
```

**После 10 Поколений (с наследованием):**
```
Богатство концентрируется у потомков богатых
Коэффициент Джини = 0.6
```

**После 10 Поколений (без наследования):**
```
Богатство остаётся в движении
Коэффициент Джини = 0.35
```

Это показывает, как наследование **стабилизирует классовые отношения** и превращает временные различия в постоянные классовые слои.

---

## 5. Социальное Воспроизводство

### 5.1. Концепция

Социальное воспроизводство — это процесс, благодаря которому **общественные отношения и классовая структура воспроизводятся из поколения в поколение**. Это происходит через:

1. **Биологическое воспроизводство** (рождение детей)
2. **Экономическое воспроизводство** (работа, производство)
3. **Социальное воспроизводство** (семья, традиции, верования)
4. **Культурное воспроизводство** (образование, идеология)

### 5.2. Цикл Социального Воспроизводства

```
ПОКОЛЕНИЕ 1
├─ Родители: класс A (землевладельцы)
├─ Дети рождаются в семье класса A
├─ Получают образование класса A
├─ Унаследуют землю класса A
└─ Сами становятся землевладельцами

        ↓ (30 лет)

ПОКОЛЕНИЕ 2
├─ Родители: класс A (землевладельцы, наследники)
├─ Дети рождаются в семье класса A
├─ Класс A ещё более укреплён
└─ Неравенство растёт

        ↓ (Но может сломаться в войне или революции)
```

### 5.3. Интеграция Всех Систем

**Цикл Симуляции в Контексте Социального Воспроизводства:**

```
Год N начинается
    ├─ ECONOMY: Производство, определение классов
    │  └─ Есть ли эксплуатация?
    ├─ SOCIETY: Демография
    │  ├─ Рождаемость зависит от еды → зависит от экономики
    │  └─ Смертность зависит от здоровья → зависит от достатка
    ├─ INHERITANCE: Когда человек умирает
    │  └─ Дети наследуют класс и собственность
    ├─ CULTURE: Верования и идеология
    │  └─ Зависят от экономического положения (базиса)
    └─ CLASS_CONSCIOUSNESS: Развитие классового сознания
       └─ Растёт при эксплуатации, падает при стабильности
```

---

## 6. Текущее Состояние (As-Is)

### Что Работает ✓

1. **Система Семей**
   - Создание семей и добавление членов
   - Отслеживание родственных связей (9 типов)
   - Регистрация браков
   - Двусторонние связи (обратные связи)

2. **Демография**
   - Расчёт рождаемости с учётом экономических факторов
   - Смертность по кривой Гомперца
   - Детская смертность
   - Смерть при родах
   - Статистика (рождения, смерти)
   - Расчёт ожидаемой продолжительности жизни

3. **Классовая Система**
   - Определение класса на основе собственности
   - Четыре основных класса (землевладельцы, безземельные, ремесленники, работники)
   - Система конфликтов между классами
   - Начальное развитие классового сознания

4. **Генетика**
   - 14 типов генов (физические + психологические)
   - Наследование от родителей
   - Мутации
   - Выражение генов с доминантностью

5. **Наследование**
   - Передача наследуемой собственности (земля, инструменты)
   - Определение наследников
   - Запись типа приобретения ("наследство")

### Ограничения и Проблемы ✗

1. **Семейная Система**
   - ❌ Нет динамического расширения семей при браках
   - ❌ Глава семьи не изменяется при смерти
   - ❌ Престиж семьи не влияет на обществе
   - ❌ Нет сложных родственных сценариев (рано разведены, вторые браки)

2. **Демография**
   - ❌ Рождение ребёнка не создаёт события в системе
   - ❌ Нет отслеживания жизненных этапов NPC в симуляции
   - ❌ Демографические факторы не полностью интегрированы с экономикой
   - ❌ Нет предсказания демографических кризисов

3. **Классовая Система**
   - ❌ Классовое сознание неполное (нет фаз развития Грамши)
   - ❌ Нет органических интеллектуалов (лидеров восстания)
   - ❌ Классовые конфликты имеют ограниченный эффект
   - ❌ Нет передачи класса по наследству (важно!)
   - ❌ Классовая борьба не влияет на революции

4. **Интеграция Систем**
   - ❌ Система семей и классов слабо связаны
   - ❌ Демография не полностью определяет классовую структуру
   - ❌ Нет механизма социальной мобильности (восхождение в класс)
   - ❌ Наследование не полностью интегрировано

---

## 7. Планируемое Состояние (To-Be)

### EPIC-SOCIAL: Социальное Воспроизводство (36 Проблем)

Данный EPIC направлен на **полное переопределение социальной системы** как целостного механизма воспроизводства общественных отношений.

**Ключевые Улучшения:**

#### 1. Интеграция Семьи и Класса (SOCIAL-001 до SOCIAL-010)
```python
# Текущее: Класс определяется независимо от семьи
current_class = determine_class(npc)

# Будущее: Класс детей унаследован от родителей
inherited_class = parent_class
potential_class = determine_class(npc)  # С шансом социальной мобильности
final_class = lerp(inherited_class, potential_class, 0.3)
```

#### 2. Полная Фаза Развития Классового Сознания (SOCIAL-011 до SOCIAL-020)
- Полная реализация 5 фаз (от отсутствия до гегемонии)
- Органические интеллектуалы (лидеры)
- Распространение идеологии через сеть
- Классовые восстания и революции

#### 3. Социальная Мобильность (SOCIAL-021 до SOCIAL-030)
```
Рождение в класс A
    ↓ (30% шанс при определённых условиях)
Восхождение в класс B
    ↓ (через брак, образование, революцию)
```

#### 4. Система Престижа и Статуса (SOCIAL-031 до SOCIAL-036)
- Престиж семьи влияет на браки и альянсы
- Статус NPC в обществе
- Влияние на принятие решений
- Репутация и слухи

**Сроки реализации:** ~4 недели (в плане Task 002)

### Ссылка на Полный EPIC

Подробное описание всех 36 проблем находится в Task 002:
📄 **EPIC_SOCIAL.md** (в спецификации Task 002 — описание всех социальных проблем и их решений)

---

## 8. Демографическая Пирамида - Визуализация

### Примеры Типичных Обществ

#### **Растущее Молодое Общество (классический треугольник)**

```
Возраст      Мужчины        Женщины
             |              |
   65+      ■ (50)         □ (55)
   60+     ■■ (150)       □□ (160)
   55+    ■■■ (300)      □□□ (310)
   50+   ■■■■ (500)     □□□□ (520)
   45+  ■■■■■ (700)    □□□□□ (750)
   40+ ■■■■■■ (900)   □□□□□□ (950)
   35+■■■■■■■ (1200)  □□□□□□□ (1300)
   30+■■■■■■■■■ (1500)□□□□□□□□□ (1600)
   25+■■■■■■■■■■■ (1800)□□□□□□□□□□□ (1900)
   20+■■■■■■■■■■■■■■ (2200)□□□□□□□□□□□□□□ (2300)
   15+■■■■■■■■■■■■■■■■■ (2800)□□□□□□□□□□□□□□□□□ (2900)
   10+■■■■■■■■■■■■■■■■■■■ (3200)□□□□□□□□□□□□□□□□□□□ (3300)
    5+■■■■■■■■■■■■■■■■■■■■■ (3500)□□□□□□□□□□□□□□□□□□□□□ (3600)
    0+■■■■■■■■■■■■■■■■■■■■■■ (3800)□□□□□□□□□□□□□□□□□□□□□□ (3900)

Всего: 28,500 (52% м, 48% ж) | Рост: +5% в год
```

#### **Стабильное Общество (прямоугольник)**

```
Возраст      Мужчины        Женщины
             |              |
   65+     ■■ (200)       □□ (220)
   60+     ■■ (210)       □□ (230)
   55+     ■■ (215)       □□ (235)
   50+     ■■ (218)       □□ (240)
   45+     ■■ (220)       □□ (245)
   40+     ■■ (222)       □□ (248)
   35+     ■■ (224)       □□ (250)
   30+     ■■ (225)       □□ (251)
   25+     ■■ (223)       □□ (248)
   20+     ■■ (220)       □□ (245)
   15+     ■■ (218)       □□ (242)
   10+     ■■ (215)       □□ (238)
    5+     ■■ (210)       □□ (230)
    0+     ■■ (200)       □□ (220)

Всего: 6,200 (51% м, 49% ж) | Рост: ~0% в год
```

#### **Сокращающееся Старое Общество (перевёрнутая пирамида)**

```
Возраст      Мужчины        Женщины
             |              |
   65+    ■■■■ (800)    □□□□ (1000)
   60+   ■■■■■ (750)   □□□□□ (900)
   55+   ■■■■ (600)    □□□□ (700)
   50+  ■■■ (400)     □□□ (500)
   45+ ■■ (300)      □□ (350)
   40+ ■■ (280)      □□ (300)
   35+ ■ (200)       □ (220)
   30+ ■ (180)       □ (200)
   25+ ■ (150)       □ (170)
   20+■ (120)        □ (130)
   15+■ (100)        □ (110)
   10+■ (80)         □ (90)
    5+■ (60)         □ (70)
    0+■ (40)         □ (50)

Всего: 5,600 (48% м, 52% ж) | Убыль: -3% в год
```

---

## 9. Заключение

Социальная система «Живого Мира» моделирует **исторический материализм в действии**:

1. **Семья** не просто биологический факт, а социальный институт, воспроизводящий классовые отношения

2. **Демография** не случайна — она определяется экономическими условиями (голод, война, достаток)

3. **Классы** не вечны — они возникают исторически из противоречий в производстве и собственности

4. **Классовое сознание** развивается поступательно — от экономического недовольства к революционному преобразованию

5. **Наследование** стабилизирует классовую структуру, создавая класс за классом

Благодаря интеграции всех этих систем, симуляция способна создавать **эмерджентные исторические процессы**: восстания, революции, смену общественных строёв — всё возникающее из объективных противоречий, а не жёсткого сценария.

---

## Ссылки на Связанную Документацию

- **[1_INTRODUCTION.md](./1_INTRODUCTION.md)** — Философия и основания проекта
- **[2_ARCHITECTURE_OVERVIEW.md](./2_ARCHITECTURE_OVERVIEW.md)** — Архитектура всей системы
- **[3_SIMULATION_CORE.md](./3_SIMULATION_CORE.md)** — Основной цикл симуляции
- **[4_ECONOMIC_SYSTEM.md](./4_ECONOMIC_SYSTEM.md)** — Экономическая база (базис)
- **[6_CULTURE_SYSTEM.md](./6_CULTURE_SYSTEM.md)** — Культурная надстройка
- **[7_NPC_SYSTEM.md](./7_NPC_SYSTEM.md)** — Система персонажей и AI
- **[11_GLOSSARY.md](./11_GLOSSARY.md)** — Глоссарий марксистских терминов
- **[10_FUTURE_STATE.md](./10_FUTURE_STATE.md)** — Планируемые улучшения (Task 002)

